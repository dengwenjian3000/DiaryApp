<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude all data from auto backup -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
